[package]
name = "alice-proxy"
version = "1.0.0"
edition = "2021"
authors = ["Moroya Sakamoto"]
license = "AGPL-3.0"
description = "L7 reverse proxy with routing, header rewriting, load balancing, and circuit breaker"

[lib]
crate-type = ["rlib"]
